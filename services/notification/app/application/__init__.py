"""Application services: use cases that compose the domain ports.

Kept separate from ``adapters`` (which know about Redis) and from ``domain`` (which knows
about nothing), matching the layering the commerce and dietary services already use.
"""
